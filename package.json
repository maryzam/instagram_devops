{
  "name": "instagram_devops",
  "version": "0.0.1",
  "description": "CI/CD pipeline for an instagram post",
  "scripts": {
    "test": "jest ./src",
    "build": "node ./build/index.js --publish=./releases/v0.1.2 --source=./src/post.json",
    "deploy": "node ./deploy/index.js --publish=./releases/0.1.2"
  },
  "author": "Mary Zam",
  "license": "MIT",
  "dependencies": {
    "canvas": "^2.8.0",
    "d3-color": "^2.0.0"
  },
  "devDependencies": {
    "jest": "^27.0.3"
  }
}
